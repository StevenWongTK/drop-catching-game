{
    "name": "my-app",
    "version": "0.1.0",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "postinstall": "(cd packages/frontend && npm install); (cd packages/backend && npm install);",
    "scripts": {
        "start": "lerna run --parallel start"
    },
    "dependencies": {
        "lerna": "^6.4.1"
    }
}
