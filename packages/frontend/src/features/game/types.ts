export interface IDrop {
    image: string
    x: number
    y: number
    size: number
    score: number
}

export interface ICatcher {
    image: string
    x: number
    y: number
    size: number
}
